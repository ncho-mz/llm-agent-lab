torch>=2.3
transformers>=4.44
# transformers 5.x는 Python 3.10+ 에서만 설치된다. 3.9에서는 4.57.x로 내려가는데 그 계열은
# huggingface-hub 1.x와 호환되지 않아, gradio가 hub 1.x를 끌어오면 import 자체가 깨진다.
huggingface-hub<1.0; python_version < "3.10"
accelerate>=0.33
peft>=0.12
trl>=0.9
datasets>=2.20
bitsandbytes>=0.43; sys_platform == "linux"
# 한국어 문서를 다루므로 다국어 임베딩 모델이 필요하다 (configs/model.yaml 참고).
# 상한을 두지 않는다 -- 예전의 <4.0 핀은 잘못된 진단(실제 원인은 Windows 콘솔 인코딩)이었다.
sentence-transformers>=3.0
chromadb>=0.5
pyyaml>=6.0
gradio>=4.44
# SSI(Single Step Instrumentation)는 쓰지 않는다 -- SSI가 주입하는 ddtrace는 PYTHONPATH를 통해
# venv 것보다 먼저 잡히기 때문에, pip show가 보여주는 버전과 실제로 도는 버전이 달라진다.
# APM은 ddtrace-run으로 이 한 벌만 쓴다. 메이저를 고정하는 건 llmobs API가 메이저에서 바뀌기 때문.
ddtrace>=4.14,<5
# 링크를 배경 지식 자료로 가져올 때 본문만 추출 (메뉴/푸터 같은 잡음 제거)
trafilatura>=1.12
